name: peekaboo_example
description: Runnable demo for the peekaboo package.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.7.2
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.0.0
  peekaboo:
    path: ..

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
